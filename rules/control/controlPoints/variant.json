{
  "subsystem": "control",
  "id": "controlPoints",
  "version": "1.0.0",
  "status": "stable",
  "description": "控制点归属：按格记录控制权 + control/capture 效果",
  "configFile": "game.json",
  "requires": ["map"],
  "provides": ["controlPoints", "control", "hexControl", "capture"],
  "configSchema": {
    "initialFromUnits": "bool 部署后按单位位置初始化控制权（默认 true）"
  }
}
